#pragma once

// Configurações de Capacidade
const int BUFFER_CAP = 2;

// Tempos de Processamento (ms) - Simulação de WCET
const int TIME_M1_PROC = 2000;
const int TIME_M2_PROC = 3500;
const int TIME_ROBOT_MOVE = 800; // Tempo de um ponto a outro
const int TIME_EXTERNAL_CONSUMER = 7000; // Consumidor retira a cada 7s